import {LoadingIcon} from "./styles"
import {Container} from "./styles"

export default function Loading({list}){


    return(
        <Container>
            <LoadingIcon/>

        </Container>

    )
}